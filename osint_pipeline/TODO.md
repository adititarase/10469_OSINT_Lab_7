# TODO List for Adding Search Functionality

- [ ] Add `search_db` function to `utils/database.py` to query the database for records matching a query string in a specified field.
- [ ] Modify `main.py` to import `search_db`, call it after running the pipeline (e.g., search for "AI" in text), and print the results in table format.
- [ ] Test the updated script to ensure the search functionality works correctly.
